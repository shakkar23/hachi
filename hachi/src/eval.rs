
use crate::state::MacroState;

use tetris::state::{State, Lock};

pub fn light_eval(_state: &State) -> i32 {
    0
}

pub fn lock_eval(_lock: &Lock) -> i32 {
    0
}

pub fn heavy_eval(_state: &MacroState) -> i32 {
    0
}